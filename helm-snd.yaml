
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-snd

env:
  APP_ENV: snd

ingress:
  enabled: true
  hosts:
    - host: "lms-lti-courselist-snd.apps-snd.iu.edu"
      paths: []

activeProfiles: "redis,snd"

serviceAccountName: lms-lti-courselist-snd

cluster: es-test

projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-stg

env:
  APP_ENV: stg

ingress:
  enabled: true
  hosts:
    - host: "lms-lti-courselist-stg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,stg"

serviceAccountName: lms-lti-courselist-stg

cluster: es-test